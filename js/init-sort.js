(function ($, root, undefined) {
	
	$(function () {
		
		'use strict';

           $('.sortable').sortable();

			
	});
	
})(jQuery, this);

function reInitSort(){
	jQuery('.sortable').sortable();
}